import configuration.StartGame;

/**
 * Class that runs the game to test the code
 */
public class Main {
    public static void main(String[] args) {
        StartGame startGame = new StartGame();
        startGame.execute();
    }
}